# Requires Python 3.12 (CI and local verification both run 3.12; 3.11 untested).

confluent-kafka==2.5.3
pydantic==2.9.2
httpx==0.27.2
python-dotenv==1.0.1
structlog==24.4.0
schedule==1.2.2
pytest==8.3.3
pytest-asyncio==0.24.0

# Phase 2 — LLM urgency classifier + tracing
langchain==0.3.7
langchain-core==0.3.21
langchain-anthropic==0.3.0
langfuse==2.55.0

# Phase 3 — Snowflake warehousing
snowflake-connector-python[pandas]==3.12.4
# snowflake-connector-python 3.12.4 imports OpenSSL at module load. Left
# unpinned, a fresh resolve picks cryptography 46 + pyOpenSSL 22, which crash
# on import (X509_V_FLAG_NOTIFY_POLICY) and break test collection. Pin the
# pair that was verified together.
cryptography==44.0.3
pyOpenSSL==24.3.0

# Phase 4 — dbt SLA analytics (pulls in dbt-core)
dbt-snowflake==1.8.4
